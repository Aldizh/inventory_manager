{
  "name": "inventory",
  "description": "Manages inventory for a gracery store",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "cd back_end && yarn test",
    "start:dev": "concurrently 'cd back_end && nodemon server.js' 'cd front_end && yarn && yarn start'",
    "start": "cd back_end && yarn && node server.js",
    "heroku-postbuild": "cd front_end && yarn && yarn build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "assert": "^1.4.1",
    "concurrently": "^5.3.0",
    "ramda": "^0.26.1",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  },
  "engines": {
    "node": "^16.3.1"
  }
}
